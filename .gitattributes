# Git attributes for consistent handling across platforms

# Auto detect text files and normalize line endings to LF in repo
* text=auto eol=lf

# Shell scripts should always use LF
*.sh text eol=lf

# Python files should always use LF
*.py text eol=lf

# Markdown files should always use LF
*.md text eol=lf

# Text files that should use LF
*.txt text eol=lf
*.json text eol=lf
*.yaml text eol=lf
*.yml text eol=lf
*.conf text eol=lf
*.ini text eol=lf

# Binary files
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.pdf binary
*.zip binary
*.tar binary
*.gz binary

# Jupyter notebooks - treat as text but don't normalize
*.ipynb text -diff

# CSV files
*.csv text eol=lf

# Dockerfiles
Dockerfile* text eol=lf

# Make scripts should use tabs
Makefile text eol=lf

# Batch files on Windows should use CRLF (if any)
*.bat text eol=crlf
*.cmd text eol=crlf
