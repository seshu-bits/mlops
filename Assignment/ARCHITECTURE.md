# Architecture Overview - Heart Disease API on Minikube

## 🏗️ System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         Alma Linux 8 Host                        │
│                                                                   │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │                      Minikube Cluster                       │ │
│  │                                                              │ │
│  │  ┌────────────────────────────────────────────────────┐   │ │
│  │  │              mlops Namespace                        │   │ │
│  │  │                                                      │   │ │
│  │  │  ┌─────────────────────────────────────────────┐  │   │ │
│  │  │  │         Service (NodePort:30080)            │  │   │ │
│  │  │  │         heart-disease-api                   │  │   │ │
│  │  │  └──────────────────┬──────────────────────────┘  │   │ │
│  │  │                     │                              │   │ │
│  │  │                     ▼                              │   │ │
│  │  │  ┌─────────────────────────────────────────────┐  │   │ │
│  │  │  │         Deployment                          │  │   │ │
│  │  │  │         (2 replicas)                        │  │   │ │
│  │  │  │                                              │  │   │ │
│  │  │  │  ┌──────────────┐  ┌──────────────┐        │  │   │ │
│  │  │  │  │   Pod 1      │  │   Pod 2      │        │  │   │ │
│  │  │  │  │              │  │              │        │  │   │ │
│  │  │  │  │ ┌──────────┐ │  │ ┌──────────┐ │        │  │   │ │
│  │  │  │  │ │FastAPI   │ │  │ │FastAPI   │ │        │  │   │ │
│  │  │  │  │ │Container │ │  │ │Container │ │        │  │   │ │
│  │  │  │  │ │          │ │  │ │          │ │        │  │   │ │
│  │  │  │  │ │Port:8000 │ │  │ │Port:8000 │ │        │  │   │ │
│  │  │  │  │ │          │ │  │ │          │ │        │  │   │ │
│  │  │  │  │ │ Model:   │ │  │ │ Model:   │ │        │  │   │ │
│  │  │  │  │ │ LR.pkl   │ │  │ │ LR.pkl   │ │        │  │   │ │
│  │  │  │  │ └──────────┘ │  │ └──────────┘ │        │  │   │ │
│  │  │  │  └──────────────┘  └──────────────┘        │  │   │ │
│  │  │  └─────────────────────────────────────────────┘  │   │ │
│  │  │                                                      │   │ │
│  │  │  Optional Components:                               │   │ │
│  │  │  • HorizontalPodAutoscaler (HPA)                   │   │ │
│  │  │  • Ingress Controller                              │   │ │
│  │  │  • ServiceMonitor (Prometheus)                     │   │ │
│  │  │  • PersistentVolumeClaim (PVC)                     │   │ │
│  │  └──────────────────────────────────────────────────┘   │ │
│  │                                                            │ │
│  │  ┌────────────────────────────────────────────────────┐  │ │
│  │  │         Minikube Internal Docker Registry          │  │ │
│  │  │         Image: heart-disease-api:latest            │  │ │
│  │  └────────────────────────────────────────────────────┘  │ │
│  └──────────────────────────────────────────────────────────┘ │
│                                                                 │
│  ┌────────────────────────────────────────────────────────┐   │
│  │               Docker Engine                             │   │
│  └────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘

External Access:
┌──────────┐
│  Client  │ ──HTTP──> http://<minikube-ip>:30080/predict
└──────────┘
```

---

## 🔄 Deployment Workflow

```
┌──────────────────┐
│  Build Docker    │
│  Image           │
│  (in Minikube)   │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│  Store in        │
│  Minikube's      │
│  Docker Registry │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│  Helm Install    │
│  Creates K8s     │
│  Resources       │
└────────┬─────────┘
         │
         ├──────────────────────┐
         │                      │
         ▼                      ▼
┌──────────────────┐  ┌──────────────────┐
│  Deployment      │  │  Service         │
│  (Pods)          │  │  (NodePort)      │
└────────┬─────────┘  └────────┬─────────┘
         │                     │
         ▼                     │
┌──────────────────┐           │
│  Pods Start      │           │
│  • Pull Image    │           │
│  • Run Container │           │
│  • Health Checks │           │
└────────┬─────────┘           │
         │                     │
         └─────────┬───────────┘
                   │
                   ▼
         ┌──────────────────┐
         │  Service Ready   │
         │  External Access │
         └──────────────────┘
```

---

## 📊 Network Flow

```
Client Request Flow:

1. Client → Minikube IP:NodePort (e.g., 192.168.49.2:30080)
2. NodePort → Service (heart-disease-api)
3. Service → LoadBalancer → Pod (Round-robin between replicas)
4. Pod → FastAPI Application
5. FastAPI → Logistic Regression Model
6. Model → Prediction
7. Response ← FastAPI ← Pod ← Service ← Client

┌─────────┐
│ Client  │
└────┬────┘
     │ HTTP Request
     │ POST /predict
     ▼
┌─────────────────┐
│ Service         │
│ (LoadBalancer)  │
└────┬────────────┘
     │
     ├──────────────┐
     │              │
     ▼              ▼
┌─────────┐   ┌─────────┐
│ Pod 1   │   │ Pod 2   │
└────┬────┘   └────┬────┘
     │              │
     │              │
     ▼              ▼
┌─────────────────────────┐
│ ML Model Processing     │
│ (Logistic Regression)   │
└─────────┬───────────────┘
          │
          ▼
     ┌─────────┐
     │Response │
     └─────────┘
```

---

## 🗂️ Helm Chart Structure

```
heart-disease-api/
│
├── Chart.yaml                 ─┐
├── values.yaml                 │ Chart Configuration
├── values-dev.yaml             │ & Values
├── values-prod.yaml           ─┘
│
└── templates/                 ─┐
    ├── _helpers.tpl            │
    ├── NOTES.txt               │
    │                           │
    ├── deployment.yaml         │ Core Resources
    ├── service.yaml            │
    ├── serviceaccount.yaml    ─┤
    │                           │
    ├── ingress.yaml            │
    ├── hpa.yaml                │ Optional/Advanced
    ├── configmap.yaml          │ Features
    ├── secret.yaml             │
    ├── pvc.yaml                │
    ├── pdb.yaml                │
    ├── networkpolicy.yaml      │
    └── servicemonitor.yaml    ─┘
```

---

## 🔐 Security Architecture

```
┌─────────────────────────────────────────────────┐
│              Security Layers                     │
│                                                  │
│  ┌────────────────────────────────────────┐    │
│  │  Network Policy                        │    │
│  │  • Ingress rules                       │    │
│  │  • Egress rules                        │    │
│  └────────────────────────────────────────┘    │
│                    │                            │
│                    ▼                            │
│  ┌────────────────────────────────────────┐    │
│  │  Service Account                       │    │
│  │  • RBAC permissions                    │    │
│  │  • Token authentication                │    │
│  └────────────────────────────────────────┘    │
│                    │                            │
│                    ▼                            │
│  ┌────────────────────────────────────────┐    │
│  │  Pod Security Context                  │    │
│  │  • RunAsNonRoot                        │    │
│  │  • ReadOnlyRootFilesystem             │    │
│  └────────────────────────────────────────┘    │
│                    │                            │
│                    ▼                            │
│  ┌────────────────────────────────────────┐    │
│  │  Container Security                    │    │
│  │  • Minimal base image                  │    │
│  │  • No privileged mode                  │    │
│  │  • Resource limits                     │    │
│  └────────────────────────────────────────┘    │
└─────────────────────────────────────────────────┘
```

---

## 📈 Scaling Architecture

```
Normal Load (2 replicas):
┌─────────┐  ┌─────────┐
│  Pod 1  │  │  Pod 2  │
└─────────┘  └─────────┘

         ▼

High Load - HPA Triggers (up to 10 replicas):
┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐
│  Pod 1  │  │  Pod 2  │  │  Pod 3  │  │  Pod 4  │
└─────────┘  └─────────┘  └─────────┘  └─────────┘
┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐
│  Pod 5  │  │  Pod 6  │  │  Pod 7  │  │  Pod 8  │
└─────────┘  └─────────┘  └─────────┘  └─────────┘
┌─────────┐  ┌─────────┐
│  Pod 9  │  │  Pod 10 │
└─────────┘  └─────────┘

Scaling Decision:
┌─────────────────┐
│ Metrics Server  │
│ • CPU: >80%     │
│ • Memory: >80%  │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│      HPA        │
│ Decision Making │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│   Deployment    │
│ Scale Up/Down   │
└─────────────────┘
```

---

## 🔍 Health Check Flow

```
Kubernetes Probes:

Startup Probe (0-150s):
┌──────────────┐
│ Pod Starting │
└──────┬───────┘
       │
       ├─ HTTP GET /health every 5s
       │
       ├─ Success → Continue
       │
       └─ Fail 30 times → Restart Pod

Liveness Probe (running):
┌──────────────┐
│  Pod Running │
└──────┬───────┘
       │
       ├─ HTTP GET /health every 10s
       │
       ├─ Success → Keep Running
       │
       └─ Fail 3 times → Restart Pod

Readiness Probe (running):
┌──────────────┐
│  Pod Running │
└──────┬───────┘
       │
       ├─ HTTP GET /health every 5s
       │
       ├─ Success → Send Traffic
       │
       └─ Fail → Remove from Service
```

---

## 📦 Container Image Structure

```
heart-disease-api:latest
│
├── python:3.11-slim (Base)
│   └── Debian-based minimal image
│
├── System Dependencies
│   └── gcc (for building Python packages)
│
├── Python Dependencies
│   ├── FastAPI
│   ├── Uvicorn
│   ├── scikit-learn
│   ├── pandas
│   ├── numpy
│   └── mlflow
│
├── Application Code
│   └── api_server.py
│
├── Model Artifacts
│   └── artifacts/
│       └── logistic_regression.pkl
│
└── Runtime Configuration
    ├── Port: 8000
    ├── Workers: Auto
    └── Host: 0.0.0.0
```

---

## 🔄 CI/CD Integration Points

```
┌──────────────┐
│  Git Commit  │
└──────┬───────┘
       │
       ▼
┌──────────────┐
│   CI Build   │
│  • Run Tests │
│  • Build IMG │
│  • Push IMG  │
└──────┬───────┘
       │
       ▼
┌──────────────┐
│  CD Deploy   │
│ helm upgrade │
└──────┬───────┘
       │
       ▼
┌──────────────┐
│  K8s Cluster │
│  • Rolling   │
│    Update    │
└──────────────┘
```

---

## 🎯 Monitoring Stack (Optional)

```
┌────────────────────────────────────────┐
│           Observability Stack          │
│                                        │
│  ┌──────────────┐  ┌──────────────┐  │
│  │  Prometheus  │←─│ServiceMonitor│  │
│  │  (Metrics)   │  └──────────────┘  │
│  └──────┬───────┘                     │
│         │                             │
│         ▼                             │
│  ┌──────────────┐                     │
│  │   Grafana    │                     │
│  │ (Dashboards) │                     │
│  └──────────────┘                     │
│                                        │
│  ┌──────────────┐                     │
│  │  Logs Stack  │                     │
│  │  • Fluentd   │                     │
│  │  • ELK/Loki  │                     │
│  └──────────────┘                     │
└────────────────────────────────────────┘
```

---

## 📊 Resource Requirements

```
Development Environment:
├── Minikube: 2 CPUs, 4GB RAM
├── Pod (per replica):
│   ├── CPU Request: 100m
│   ├── CPU Limit: 500m
│   ├── Memory Request: 128Mi
│   └── Memory Limit: 512Mi
└── Total (2 replicas): ~1GB RAM, 1 CPU

Production Environment:
├── Minikube: 4 CPUs, 8GB RAM
├── Pod (per replica):
│   ├── CPU Request: 500m
│   ├── CPU Limit: 1000m
│   ├── Memory Request: 512Mi
│   └── Memory Limit: 1Gi
└── Total (3-10 replicas): 1.5GB-10GB RAM, 1.5-10 CPUs
```

---

## 🚀 Deployment Timeline

```
Initial Setup (First Time):
├── Install Docker: ~5 min
├── Install kubectl: ~2 min
├── Install Minikube: ~2 min
├── Install Helm: ~2 min
├── Start Minikube: ~2 min
└── Total: ~13 minutes

Regular Deployment:
├── Build Image: ~1 min
├── Helm Install: ~30 sec
├── Pod Startup: ~30 sec
├── Health Checks: ~30 sec
└── Total: ~2.5 minutes

Update Deployment:
├── Helm Upgrade: ~10 sec
├── Rolling Update: ~1 min
├── Health Checks: ~30 sec
└── Total: ~1.7 minutes
```

---

This architecture provides a robust, scalable, and production-ready deployment
of your Heart Disease Prediction API on Kubernetes/Minikube!
