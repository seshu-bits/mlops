╔══════════════════════════════════════════════════════════════════════════════╗
║                  ALMA LINUX 8 DEPLOYMENT - QUICK REFERENCE                   ║
║                    MLOps Application on 72.163.219.91                        ║
╚══════════════════════════════════════════════════════════════════════════════╝

┌─────────────────────────────────────────────────────────────────────────────┐
│ 🚀 ONE-COMMAND DEPLOYMENT                                                   │
└─────────────────────────────────────────────────────────────────────────────┘

  git clone https://github.com/seshu-bits/mlops.git
  cd mlops/Assignment
  ./deploy-complete-almalinux.sh

  Duration: ~10-15 minutes
  Result: All services deployed and accessible remotely

┌─────────────────────────────────────────────────────────────────────────────┐
│ 🌐 ACCESS URLS                                                              │
└─────────────────────────────────────────────────────────────────────────────┘

  API:        http://72.163.219.91/
  API Docs:   http://72.163.219.91/docs
  Prometheus: http://72.163.219.91:9090
  Grafana:    http://72.163.219.91:3000  (admin/admin)
  MLflow:     http://72.163.219.91:5000

┌─────────────────────────────────────────────────────────────────────────────┐
│ 🧪 TEST DEPLOYMENT                                                          │
└─────────────────────────────────────────────────────────────────────────────┘

  ./test-deployment.sh                      # Run all tests

  curl http://72.163.219.91/health          # Test API health
  curl http://72.163.219.91/metrics         # Check metrics

┌─────────────────────────────────────────────────────────────────────────────┐
│ 📊 QUICK STATUS CHECKS                                                      │
└─────────────────────────────────────────────────────────────────────────────┘

  kubectl get pods -n mlops                 # View all pods
  kubectl get svc -n mlops                  # View services
  kubectl get ingress -n mlops              # View ingress
  kubectl logs -n mlops <pod-name>          # View logs
  kubectl logs -n mlops -l app.kubernetes.io/name=heart-disease-api -f

┌─────────────────────────────────────────────────────────────────────────────┐
│ 🔧 COMMON OPERATIONS                                                        │
└─────────────────────────────────────────────────────────────────────────────┘

  # Scale API
  kubectl scale deployment heart-disease-api --replicas=3 -n mlops

  # Restart API
  kubectl rollout restart deployment heart-disease-api -n mlops

  # Restart all services
  kubectl rollout restart deployment -n mlops

  # Update application
  eval $(minikube docker-env)
  docker build -t heart-disease-api:latest -f Dockerfile.almalinux .
  kubectl rollout restart deployment heart-disease-api -n mlops

┌─────────────────────────────────────────────────────────────────────────────┐
│ 🐛 TROUBLESHOOTING                                                          │
└─────────────────────────────────────────────────────────────────────────────┘

  # Check Minikube
  minikube status
  minikube start                            # If stopped

  # Check tunnel (must run continuously)
  ps aux | grep "minikube tunnel"
  nohup minikube tunnel > /tmp/minikube-tunnel.log 2>&1 &

  # Check Nginx
  sudo systemctl status nginx
  sudo systemctl restart nginx

  # Check firewall
  sudo firewall-cmd --list-all
  sudo firewall-cmd --permanent --add-port={80,3000,5000,9090}/tcp
  sudo firewall-cmd --reload

  # Check pod issues
  kubectl describe pod <pod-name> -n mlops
  kubectl get events -n mlops --sort-by='.lastTimestamp'

┌─────────────────────────────────────────────────────────────────────────────┐
│ 📝 API USAGE EXAMPLES                                                       │
└─────────────────────────────────────────────────────────────────────────────┘

  # Health check
  curl http://72.163.219.91/health

  # Single prediction
  curl -X POST http://72.163.219.91/predict \
    -H "Content-Type: application/json" \
    -d '{
      "age": 63, "sex": 1, "cp": 3, "trestbps": 145, "chol": 233,
      "fbs": 1, "restecg": 0, "thalach": 150, "exang": 0,
      "oldpeak": 2.3, "slope": 0, "ca": 0, "thal": 1
    }'

  # Batch prediction
  curl -X POST http://72.163.219.91/predict/batch \
    -H "Content-Type: application/json" \
    -d @sample_batch_input.json

┌─────────────────────────────────────────────────────────────────────────────┐
│ 🗑️ CLEANUP                                                                  │
└─────────────────────────────────────────────────────────────────────────────┘

  ./cleanup-deployment.sh                   # Interactive cleanup

  # Quick cleanup
  kubectl delete namespace mlops
  pkill -f "minikube tunnel"

  # Full cleanup (including Minikube)
  minikube delete
  docker system prune -a

┌─────────────────────────────────────────────────────────────────────────────┐
│ 📚 DOCUMENTATION                                                            │
└─────────────────────────────────────────────────────────────────────────────┘

  ALMALINUX_DEPLOYMENT_README.md            # Start here (overview)
  QUICKSTART_ALMALINUX.md                   # Quick reference guide
  ALMA_LINUX_COMPLETE_DEPLOYMENT.md         # Detailed manual deployment
  DEPLOYMENT_SUMMARY.md                     # Technical reference
  DEPLOYMENT_CHECKLIST.md                   # Verification checklist

┌─────────────────────────────────────────────────────────────────────────────┐
│ 🔄 AFTER SERVER REBOOT                                                      │
└─────────────────────────────────────────────────────────────────────────────┘

  minikube start
  nohup minikube tunnel > /tmp/minikube-tunnel.log 2>&1 &
  kubectl get pods -n mlops                 # Verify all running
  sudo systemctl start nginx                # If needed

┌─────────────────────────────────────────────────────────────────────────────┐
│ 🎯 ARCHITECTURE                                                             │
└─────────────────────────────────────────────────────────────────────────────┘

  Internet → Nginx (Alma Server) → Minikube Tunnel → Kubernetes Ingress
    → Services (API, Prometheus, Grafana, MLflow)

  Components:
  • API: 2 replicas, FastAPI + ML model
  • Prometheus: Metrics collection (port 9090)
  • Grafana: Visualization (port 3000)
  • MLflow: Experiment tracking (port 5000)
  • Ingress: nginx-ingress-controller

┌─────────────────────────────────────────────────────────────────────────────┐
│ ⚙️ CONFIGURATION FILES                                                      │
└─────────────────────────────────────────────────────────────────────────────┘

  deploy-complete-almalinux.sh              # Main deployment script
  test-deployment.sh                        # Testing script
  cleanup-deployment.sh                     # Cleanup script

  helm-charts/heart-disease-api/            # API Helm chart
  monitoring/prometheus-deployment.yaml     # Prometheus config
  monitoring/grafana-deployment.yaml        # Grafana config
  monitoring/grafana-dashboard.json         # Dashboard config

  Nginx config: /etc/nginx/conf.d/mlops-proxy.conf

┌─────────────────────────────────────────────────────────────────────────────┐
│ 📊 MONITORING QUERIES (Prometheus)                                         │
└─────────────────────────────────────────────────────────────────────────────┘

  Access: http://72.163.219.91:9090

  # Request rate
  rate(api_requests_total[5m])

  # Average latency
  rate(prediction_duration_seconds_sum[5m]) / 
    rate(prediction_duration_seconds_count[5m])

  # Error rate
  rate(api_errors_total[5m])

  # Active requests
  active_requests

  # Model status
  model_loaded

┌─────────────────────────────────────────────────────────────────────────────┐
│ 🎨 GRAFANA SETUP                                                            │
└─────────────────────────────────────────────────────────────────────────────┘

  1. Access: http://72.163.219.91:3000
  2. Login: admin/admin
  3. Navigate: Dashboards → Import
  4. Upload: monitoring/grafana-dashboard.json
  5. Select: Prometheus datasource

┌─────────────────────────────────────────────────────────────────────────────┐
│ ⚡ QUICK DIAGNOSTICS                                                        │
└─────────────────────────────────────────────────────────────────────────────┘

  # Check all components at once
  kubectl get all -n mlops

  # Resource usage
  kubectl top nodes
  kubectl top pods -n mlops

  # Comprehensive test
  ./test-deployment.sh

  # Check Minikube IP
  minikube ip

  # View full logs
  kubectl logs -n mlops --all-containers=true --tail=100

┌─────────────────────────────────────────────────────────────────────────────┐
│ 🔐 SECURITY NOTES                                                           │
└─────────────────────────────────────────────────────────────────────────────┘

  • Change Grafana password: admin/admin (default)
  • Firewall: Only required ports open (80, 3000, 5000, 9090)
  • For production: Add TLS, authentication, network policies
  • Regular updates: Pull latest code, rebuild images

┌─────────────────────────────────────────────────────────────────────────────┐
│ 📞 SUPPORT                                                                  │
└─────────────────────────────────────────────────────────────────────────────┘

  Repository: https://github.com/seshu-bits/mlops
  Issues: Open GitHub issue
  Logs: kubectl logs -n mlops <pod-name>
  Test: ./test-deployment.sh

┌─────────────────────────────────────────────────────────────────────────────┐
│ ✅ SUCCESS CRITERIA                                                         │
└─────────────────────────────────────────────────────────────────────────────┘

  ✓ All pods Running: kubectl get pods -n mlops
  ✓ API responds: curl http://72.163.219.91/health
  ✓ Predictions work: curl http://72.163.219.91/docs
  ✓ Metrics collected: curl http://72.163.219.91/metrics
  ✓ UI accessible: http://72.163.219.91:3000
  ✓ Tests pass: ./test-deployment.sh (0 failures)

╔══════════════════════════════════════════════════════════════════════════════╗
║                  DEPLOYMENT COMPLETE - ALL SYSTEMS GO! 🚀                    ║
╚══════════════════════════════════════════════════════════════════════════════╝

Keep this reference handy for quick lookups during operations and troubleshooting!

Server: 72.163.219.91
Version: 1.0.0
Date: January 7, 2026
